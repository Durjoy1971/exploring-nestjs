info:
  name: GET ENV VAR
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/users/2/welcome
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
