info:
  name: update post
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:3000/posts/1
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit
  body:
    type: json
    data: |-
      {
        "description": "Explaining validation DTOs in NestJS."
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
