info:
  name: params
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/hello/query?name=Durjoy
  params:
    - name: name
      value: Durjoy
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
